---
import { personalInfo } from '@/data/personal';
import { Image } from 'astro:assets';
---

<section id="about-me" class="py-20 bg-slate-800">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="animate-on-scroll">
      <div class="text-center mb-16">
        <h2 class="text-3xl md:text-4xl font-bold text-green-400 mb-4">
          Sobre Mí
        </h2>
        <div class="w-24 h-1 bg-green-500 mx-auto"></div>
      </div>

      <div class="grid md:grid-cols-2 gap-12 items-center">
        <!-- Imagen optimizada -->
        <div class="order-2 md:order-1">
          <div class="relative max-w-md mx-auto">
            <Image 
              src={personalInfo.aboutMeImage || "/placeholder.svg"} 
              alt={`${personalInfo.name} - Foto de perfil`}
              width={400}
              height={400}
              class="rounded-2xl shadow-2xl w-full h-auto object-cover"
              loading="lazy"
              format="webp"
              quality={85}
            />
            <!-- Efecto de fondo -->
            <div class="absolute bg-gradient-to-r from-green-500 to-green-600 rounded-2xl opacity-20 blur-xl"></div>
          </div>
        </div>

        <!-- Contenido -->
        <div class="order-1 md:order-2 space-y-6">
          <div class="prose prose-lg prose-invert max-w-none">
            <p class="text-gray-300 leading-relaxed">
              {personalInfo.bio}
            </p>
            
            <p class="text-gray-300 leading-relaxed">
              Mi enfoque se centra en escribir código limpio, escalable y mantenible, 
              siempre buscando las mejores prácticas y las tecnologías más actuales. 
              Me apasiona resolver problemas complejos y transformar ideas en soluciones digitales efectivas.
            </p>

            <p class="text-gray-300 leading-relaxed">
              Cuando no estoy programando, disfruto mucho de jugar ajedrez, ver películas y series, 
              y salir a caminar por la ciudad a lugares aleatorios.
            </p>
          </div>

          <!-- Información personal -->
          <div class="grid grid-cols-1 sm:grid-cols-2 gap-4 pt-6">
            <div class="bg-slate-700 p-4 rounded-lg">
              <h4 class="text-green-400 font-semibold mb-2">Ubicación</h4>
              <p class="text-gray-300">{personalInfo.location}</p>
            </div>
            <div class="bg-slate-700 p-4 rounded-lg">
              <h4 class="text-green-400 font-semibold mb-2">Email</h4>
              <p class="text-gray-300">{personalInfo.email}</p>
            </div>
          </div>

          <!-- Botón CV -->
          <div class="pt-6">
            <a 
              href="/cv.pdf" 
              download
              class="inline-flex items-center bg-green-600 hover:bg-green-700 text-white px-6 py-3 rounded-lg font-medium transition-all duration-300 hover:scale-105 hover:shadow-lg hover:shadow-green-500/25"
            >
              <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 28 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 10v6m0 0l-3-3m3 3l3-3m2 8H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
              </svg>
              Descargar CV
            </a>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
